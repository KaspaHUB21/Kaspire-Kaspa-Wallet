import type { KaspaNetwork } from "../shared/protocol";
export interface WalletAddress { address:string;name:string;path:string;watchOnly:boolean;walletId:string;coinType:number;account:number;change:number;index:number }
export interface Contact{id:string;name:string;address:string}
export interface DappPermission {accounts:boolean;connectedAt:number;networks?:KaspaNetwork[];evmChainId?:number}
export interface WalletState { version:2;network:KaspaNetwork;selectedAddress:string|null;addresses:WalletAddress[];permissions:Record<string,DappPermission>;contacts:Contact[];settings:{autoLockMinutes:number;hideBalances:boolean;currency:string;uppercase:boolean;theme:string;showSubwallets:boolean;recipientAllowlist:boolean};locked:boolean;recoveryVerified:boolean }
const defaults:WalletState={version:2,network:"mainnet",selectedAddress:null,addresses:[],permissions:{},contacts:[],settings:{autoLockMinutes:15,hideBalances:false,currency:"USD",uppercase:true,theme:"midnight",showSubwallets:true,recipientAllowlist:false},locked:true,recoveryVerified:true};
export async function loadState():Promise<WalletState>{const{walletState}=await chrome.storage.local.get("walletState");if(walletState?.version===2)return{...defaults,...walletState,settings:{...defaults.settings,...walletState.settings},locked:true};return{...defaults}}
export async function saveState(state:WalletState){const{walletState:previous}=await chrome.storage.local.get("walletState");const current={...state,locked:true};await chrome.storage.local.set({walletState:current});void chrome.tabs.query({}).then(tabs=>Promise.all(tabs.flatMap(tab=>tab.id===undefined?[]:[chrome.tabs.sendMessage(tab.id,{kind:"providerStateChanged",previous:previous??null,current}).catch(()=>undefined)])))}
